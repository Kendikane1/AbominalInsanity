# AI project bootstrap conventions

The `ai-project` command creates the filesystem and lets `uv` generate and update
the dependency manifest. The following files are copied or added by the workspace
setup after initialization.

This directory documents the intended generated layout; it is not itself a
Python project.
