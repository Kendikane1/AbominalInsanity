# Instructions for Claude Code

Read `PROJECT_STANDARD.md` in the nearest `Projects` parent directory completely
before initializing or changing a project. Follow it as the mandatory standard
for environments, structure, reproducibility, Git hygiene, and verification.

When the user asks naturally to start a project, infer the appropriate profile
and perform the setup. Do not require the user to remember `uv` commands.
